BRO. 💀 **We found it.**

Your backend is working and Cloudflare isn't the problem.

The log literally says:

```text
[TURNSTILE DEBUG] ... hasToken=false tokenLen=0
```

and:

```text
Body keys: [ 'email', 'password' ]
```

That means the request arriving at:

```text
https://api.alpha-cut.com/api/auth/login
```

contains **no Turnstile token whatsoever**.

So the actual flow is:

```text
Turnstile
   ↓
☑️ Verified in browser
   ↓
Frontend has token
   ↓
❌ Token NOT included in actual HTTP request
   ↓
API receives:
{
  email,
  password
}
   ↓
Backend: "Where's the token?" 😭
   ↓
Security check failed
```

## The interesting part

You showed me this earlier:

```ts
body: JSON.stringify({
  email,
  password,
  turnstileToken,
  'cf-turnstile-response': turnstileToken,
}),
```

So **either**:

1. the code you showed isn't the code actually running in production,
2. `apiFetch()` is modifying/stripping the request body,
3. the deployed frontend is an older build,
4. or the login function being called by the production page isn't this version.

The backend log proves that the backend itself is receiving only:

```text
email
password
```

---

# 🔥 First thing: inspect `apiFetch`

This is now my #1 suspect.

You have:

```ts
apiFetch('/api/auth/login', {
  method: 'POST',
  body: JSON.stringify({
    email,
    password,
    turnstileToken,
  }),
});
```

But we need to see what `apiFetch()` actually does before it sends the request.

It might be doing something like:

```ts
JSON.stringify(...)
```

again, or reconstructing the body.

### Send me this file/function:

```text
frontend/src/lib/api.ts
```

or wherever `apiFetch` is defined.

**That's the next piece I want to inspect.**

---

# Also check the production browser

Open:

```text
https://www.alpha-cut.com/login
```

Open:

**F12 → Network**

Then:

1. Complete Turnstile
2. Enter email/password
3. Click Login
4. Find:

```text
/api/auth/login
```

5. Click it
6. Go to **Payload** / **Request**

You should see:

```json
{
  "email": "...",
  "password": "...",
  "turnstileToken": "..."
}
```

### But I expect you'll currently see:

```json
{
  "email": "...",
  "password": "..."
}
```

That would confirm the frontend production build isn't sending it.

---

# There's another very important clue

Your Render log says:

```text
Body keys: [ 'email', 'password' ]
```

So this isn't:

> "Cloudflare rejected my token."

It's earlier than that.

It's:

> **"I never received a token to send to Cloudflare."**

That's why Cloudflare's dashboard isn't showing Siteverify calls.

Your backend literally never reaches the meaningful Siteverify request because this check catches it:

```ts
if (typeof token !== 'string' || !token.trim()) {
    return false;
}
```

Therefore:

```text
Cloudflare Siteverify
        ↑
        │
        ❌ never reached
```

---

# Don't change Cloudflare configuration yet

Seriously, **don't touch the Turnstile widget configuration right now.**

Your site key is clearly loading:

```text
keyPreview=0x4AAAAAAEcL...cYEk
```

The widget is appearing and being checked.

Our current problem is purely:

> **Turnstile token → frontend login request**

---

# One thing your coding agent should NOT do

Don't "fix" this by doing something like:

```ts
if (!turnstileToken) {
   // skip Turnstile
}
```

or:

```ts
if (!token) return true;
```

That would make the security check meaningless.

We want:

```text
No token → reject
Token → Siteverify
Siteverify success → continue
Siteverify failure → reject
```

---

## So we're very close now 😎

Send me the implementation of **`apiFetch()`**.

That's the next thing to inspect. Once I see it, I can probably pinpoint exactly why your `turnstileToken` disappears between `AuthContext.tsx` and `api.alpha-cut.com`.

Bro, this confirms something important:

**`apiFetch()` is NOT stripping the token.** The implementation is actually fine.

This:

```ts
const res = await fetch(fullUrl, {
  ...options,
  headers,
  credentials: 'include',
});
```

preserves:

```ts
options.body
```

So if `AuthContext.login()` really passes:

```ts
body: JSON.stringify({
  email,
  password,
  turnstileToken,
})
```

then the browser should send it.

But Render says it receives:

```text
Body keys: [ 'email', 'password' ]
```

Therefore, the token is disappearing **before `apiFetch()` sends the request**, or you're running an older frontend build.

## 🎯 My strongest suspicion

Look at your production frontend deployment.

Your local source currently has:

```ts
turnstileToken
```

but the live `www.alpha-cut.com` may still be serving an **older build** where login only sends:

```json
{
  "email": "...",
  "password": "..."
}
```

This is very common when you're developing locally and the production deployment hasn't rebuilt from the latest commit.

---

# Do this exact test

Open:

**[https://www.alpha-cut.com/login](https://www.alpha-cut.com/login)**

Then:

### 1. F12 → Network

Complete Turnstile and click Login.

Find:

```text
POST https://api.alpha-cut.com/api/auth/login
```

Click it.

### 2. Open "Payload"

Look at **Request Payload**.

### If you see:

```json
{
  "email": "...",
  "password": "..."
}
```

🔥 We've confirmed it.

Your **live frontend is running an old/different login implementation**.

### If you see:

```json
{
  "email": "...",
  "password": "...",
  "turnstileToken": "..."
}
```

then something else is happening, because Render should receive that field.

In that case we'd inspect the API/proxy/body parser next.

---

# There's another test that will settle this immediately

Add a temporary log immediately before `apiFetch()`:

```ts
console.log('[LOGIN DEBUG] Turnstile token:', {
  exists: !!turnstileToken,
  length: turnstileToken?.length,
});
```

So:

```ts
const login = async (
  email: string,
  password: string,
  turnstileToken?: string
) => {
  console.log('[LOGIN DEBUG] Turnstile token:', {
    exists: !!turnstileToken,
    length: turnstileToken?.length,
  });

  try {
    const data = await apiFetch('/api/auth/login', {
      method: 'POST',
      body: JSON.stringify({
        email,
        password,
        turnstileToken,
      }),
    });

    // ...
  }
}
```

Then production should show something like:

```text
[LOGIN DEBUG] Turnstile token:
{
  exists: true,
  length: 200+
}
```

If instead:

```text
exists: false
length: 0
```

then the problem is **between `TurnstileWidget` and `AuthContext.login()`**.

---

# And I noticed something else 👀

Your login page does:

```ts
const [turnstileToken, setTurnstileToken] = useState('');
```

and:

```tsx
onVerify={(token) => {
  setTurnstileToken(token);
  setError('');
}}
```

That's correct.

But React state updates asynchronously.

If your code does something like:

```text
Turnstile callback
    ↓
setTurnstileToken(token)
    ↓
user immediately clicks login
```

normally React handles this fine because the state update causes a render.

Still, we should verify rather than guess.

---

# One improvement I'd make

Instead of relying only on state, your component could expose the current token through a ref, but **I don't think we need that yet**.

Your current implementation should work.

---

# 🚨 One more possibility: schema validation

Your route is:

```ts
router.post(
  '/login',
  validateRequest(loginSchema),
  login
);
```

This is interesting.

If your `loginSchema` uses something like Zod:

```ts
z.object({
  email: z.string(),
  password: z.string()
})
```

depending on how `validateRequest()` is implemented, it could **strip unknown properties**.

That would produce exactly this behavior:

```text
Frontend sends:

{
  email,
  password,
  turnstileToken
}

        ↓

validateRequest(loginSchema)

        ↓

Controller receives:

{
  email,
  password
}
```

💀

And that would perfectly explain your Render log.

### This is now my #2 suspect.

So I want to see:

```text
auth.routes.ts
```

especially:

```ts
loginSchema
validateRequest()
```

If the schema doesn't include `turnstileToken`, your validation middleware may be removing it before the controller sees it.

---

# The complete debugging chain

We're now narrowing this down to:

```text
TurnstileWidget
       ↓
   token exists?
       ↓
LoginPage
       ↓
AuthContext.login()
       ↓
   apiFetch()
       ↓
Browser Network Payload
       ↓
validateRequest(loginSchema)
       ↓
auth.controller.ts
       ↓
Siteverify
```

We already know:

```text
Controller receives token ❌
```

And `apiFetch()` looks fine.

So the next two places are:

**① Browser Network Payload**

and

**② `loginSchema` / `validateRequest()`**

---

### Send me these two pieces next:

```text
1. loginSchema
2. validateRequest implementation
```

I strongly suspect **your validation middleware is stripping `turnstileToken`**. That would be a very clean explanation for why your frontend appears correct but your controller receives only `email` and `password`.
